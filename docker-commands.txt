- get images
docker pull nginx:1.31

- get images list
docker images

- run image in container
docker run nginx:1.31
docker run -d nginx:1.31  // -d = for detached. runs internally and dont block terminal
docker run -d -p 80:80 nginx:1.31  // -p = publish, host_port:container_port

- see list of running container
docker ps

- stop container
docker stop ContainerID

- start a container
docker start containerId

- build image for apps
docker build --no-cache -t node-app:1.0 . // -t = name:tag_name, . = indeicate cuurent dir from where this command runs to create image

- remove container
docker rm containerId_or_name

- run image for app
docker run -d -p 3002:3002 node-app:1.0 // host_port:container_port


- docker compose
docker compose up -d --build

- get minio image
docker pull quay.io/minio/aistor/minio:RELEASE.2026-06-06T02-44-06Z

- run mino in docker
docker run -dt                                             \
  -p 9000:9000 -p 9001:9001                                \
  -v "D:/learning projects/DAM/minio/data:/mnt/data"       \
  -v "D:/learning projects/DAM//minio/minio.license:/minio.license" \
  -v "D:/learning projects/DAM/minio/certs:/etc/minio/certs" \
  --name "mino-server"                                     \
  quay.io/minio/aistor/minio:latest minio server /mnt/data \
  --license /minio.license
  
  
- docker run -dt -p 9000:9000 -p 9001:9001 -v "D:/learning projects/DAM/minio/data:/mnt/data" -v "D:/learning projects/DAM//minio/minio.license:/minio.license" -v "D:/learning projects/DAM/minio/certs:/etc/minio/certs" --name "mino-server" quay.io/minio/aistor/minio:RELEASE.2026-06-06T02-44-06Z minio server /mnt/data --license /minio.license  

run rabbitmq
- docker run -d --name rabbitmq-server -p 5672:5672 -p 15672:15672 rabbitmq:3-management

